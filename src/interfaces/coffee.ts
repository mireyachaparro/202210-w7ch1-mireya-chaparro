export type ProtoCoffee = {
  name?: string;
  color?: string;
  taste?: string;
  isExtra?: boolean;
};

export type Coffee = {
  id: string;
  name: string;
  color: string;
  taste: string;
  isExtra: boolean;
};
